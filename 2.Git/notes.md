**Basic cmd-line instructions**
```
git clone https://github.com/isofnothing/hello ->ls

cd hello->touch hello.html->code .(write code in hello.html)
->git add hello.html/git add .->git commit -m "Add hello.html"
->git push 

write code in github -> git pull

git status/log

git commit -> -am "message"
```

<br>

**Merge conflict**
```
find merge conflict(usually in the progress of push/pull)
-> fix merge conflict 
-> git commit -m "Fix merge conflict"

git merge style ->-> fix merge conflict
-> git commit -m "Fix merge conflict"
```

<br>

**Branch**
```
git branch

git checkout -b style
git chechout main
```

<br>

**Fork**
```
fork -> make changes -> pull request
```

<br>

**GitHub Pages**
```
create a repo(named username.github.io)
-> create a index.html
-> git commit -m "First commit"
```

<br>

**Something else**
```
install git -> configure
    git config --global user.name "isofnothing"
    git config --global user.email huangruijun424@gmail.com

two ways:
    locally edit -> commit -> publish branch
    create folder -> add remote ->publish branch

dir(git:ls)
echo. > filename.text(git:touch)

If you didn’t create a README
this link will appear near the top of the page in the “Quick Setup” section.

right button + open git bash

unload:clean path -> control panel/program
```
